//! AC-53: Bubblewrap (`bwrap`) namespace wrapper.
//!
//! Stub — populated in Slice A2b.3. Will compose user + pid + net +
//! mount + uts + ipc + cgroup namespaces and hand the resulting
//! command off to the next sandbox layer.
