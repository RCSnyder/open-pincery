//! AC-53: landlock LSM filesystem ruleset.
//!
//! Stub — populated in Slice A2b.4. Will build an
//! `AccessFs::from_all(ABI::V3)` ruleset restricting reads/writes
//! to the workspace scratch directory and the tool invocation's
//! declared inputs.
